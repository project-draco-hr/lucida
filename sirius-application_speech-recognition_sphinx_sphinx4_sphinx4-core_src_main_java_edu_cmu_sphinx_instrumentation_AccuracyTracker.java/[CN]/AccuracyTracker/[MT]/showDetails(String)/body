{
  if (showDetails) {
    System.out.println();
    aligner.printSentenceSummary();
    if (showRaw) {
      System.out.println("RAW     " + rawText);
    }
    System.out.println();
    aligner.printTotalSummary();
  }
}
