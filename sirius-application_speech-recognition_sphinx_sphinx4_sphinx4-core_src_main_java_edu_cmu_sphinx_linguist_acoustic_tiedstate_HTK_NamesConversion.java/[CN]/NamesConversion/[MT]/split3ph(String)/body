{
  int i=nom.indexOf('-');
  if (i >= 0) {
    left=nom.substring(0,i);
  }
 else {
    left=null;
    i=-1;
  }
  String s=nom.substring(i + 1);
  i=s.indexOf('+');
  if (i >= 0) {
    right=s.substring(i + 1);
  }
 else {
    right=null;
    i=s.length();
  }
  base=s.substring(0,i);
}
