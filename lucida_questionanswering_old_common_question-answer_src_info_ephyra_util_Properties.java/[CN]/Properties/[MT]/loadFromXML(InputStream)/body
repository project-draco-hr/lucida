{
  java.util.Properties properties=new java.util.Properties();
  properties.loadFromXML(is);
  for (  Map.Entry entry : properties.entrySet())   put((String)entry.getKey(),(String)entry.getValue());
}
