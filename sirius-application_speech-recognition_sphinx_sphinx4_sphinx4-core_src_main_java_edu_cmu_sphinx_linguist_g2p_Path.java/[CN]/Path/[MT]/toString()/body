{
  StringBuilder sb=new StringBuilder();
  sb.append(cost + "\t");
  for (  String s : path) {
    sb.append(s);
    if (!s.equals(path.get(path.size() - 1))) {
      sb.append(" ");
    }
  }
  return sb.toString();
}
