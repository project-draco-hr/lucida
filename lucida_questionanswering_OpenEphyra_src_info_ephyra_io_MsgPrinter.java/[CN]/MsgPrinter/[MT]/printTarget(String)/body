{
  if (statusMsgs) {
    target="Target: " + target;
    String line="";
    for (int i=0; i < target.length(); i++)     line+="=";
    printMessage("\n" + line);
    printMessage(target);
    printMessage(line);
  }
}
