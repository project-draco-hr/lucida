{
  this.LUCID=LUCID;
  Indri_path=System.getenv("LUCIDAROOT") + "/questionanswering/OpenEphyra/db/" + LUCID;
  if (collection.find(eq("LUCID",LUCID)).first() == null) {
    collection.insertOne(new Document(new Document("LUCID",LUCID)));
  }
}
