{
  if (level == 0) {
    return "";
  }
 else {
    String[] split=labelName.split("\\.");
    String prefix=split[0];
    for (int i=1; i < level; i++) {
      prefix+="." + split[i];
    }
    return prefix;
  }
}
