{
  List<Integer> ints=new ArrayList<Integer>();
  for (  String token : getTokenList("[","]"))   ints.add(Integer.parseInt(token));
  int[] result=new int[ints.size()];
  for (int i=0; i < result.length; ++i)   result[i]=ints.get(i);
  return result;
}
