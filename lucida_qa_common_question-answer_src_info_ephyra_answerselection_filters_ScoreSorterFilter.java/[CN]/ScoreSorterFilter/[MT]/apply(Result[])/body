{
  for (  Result result : results)   result.setScore(result.getScore() * -1);
  Arrays.sort(results);
  for (  Result result : results)   result.setScore(result.getScore() * -1);
  return results;
}
