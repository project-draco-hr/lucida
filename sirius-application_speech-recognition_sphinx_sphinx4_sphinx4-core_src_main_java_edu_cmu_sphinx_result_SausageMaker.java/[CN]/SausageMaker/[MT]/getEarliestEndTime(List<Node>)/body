{
  if (cluster.isEmpty()) {
    return -1;
  }
  int endTime=Integer.MAX_VALUE;
  for (  Node n : cluster) {
    if (n.getEndTime() < endTime) {
      endTime=n.getEndTime();
    }
  }
  return endTime;
}
