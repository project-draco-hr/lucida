{
  Iterator<SingleHMM> it=new Iterator<SingleHMM>(){
    int cur;
    public void remove(){
    }
    public SingleHMM next(){
      for (; ; ) {
        if (cur >= hmms.size())         return null;
        SingleHMM hmm=hmms.get(cur++);
        if (!(hmm.getName().indexOf('-') >= 0 || hmm.getName().indexOf('+') >= 0))         continue;
        return hmm;
      }
    }
    public boolean hasNext(){
      return false;
    }
  }
;
  return it;
}
