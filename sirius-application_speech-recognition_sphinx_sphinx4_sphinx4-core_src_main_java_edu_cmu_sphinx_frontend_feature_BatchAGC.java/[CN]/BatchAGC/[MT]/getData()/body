{
  Data output=null;
  if (!cepstraList.isEmpty()) {
    output=cepstraList.remove(0);
  }
 else {
    agc=0.0;
    cepstraList.clear();
    if (readUtterance() > 0) {
      normalizeList();
      output=cepstraList.remove(0);
    }
  }
  return output;
}
