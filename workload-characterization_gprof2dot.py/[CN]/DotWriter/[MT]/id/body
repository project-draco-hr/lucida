def id(self, id):
    if isinstance(id, (int, float)):
        s = str(id)
    elif isinstance(id, basestring):
        if (id.isalnum() and (not id.startswith('0x'))):
            s = id
        else:
            s = self.escape(id)
    else:
        raise TypeError
    self.write(s)
