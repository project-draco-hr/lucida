{
  if (cluster.isEmpty()) {
    return -1;
  }
  int startTime=0;
  for (  Node n : cluster) {
    if (n.getBeginTime() > startTime) {
      startTime=n.getBeginTime();
    }
  }
  return startTime;
}
