{
  try {
    if (locateField(bytes,fieldIdPathFirst,fieldIdPathRest) != null) {
      tb.read(protocol_);
    }
  }
 catch (  Exception e) {
    throw new TException(e);
  }
 finally {
    trans_.clear();
    protocol_.reset();
  }
}
