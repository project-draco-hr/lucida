{
  int count=0;
  Iterator<ConfusionSet> i=iterator();
  while (i.hasNext()) {
    ConfusionSet cs=i.next();
    for (    Set<WordResult> words : cs.values()) {
      count+=words.size();
    }
  }
  return count;
}
