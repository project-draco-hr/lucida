{
  File[] newFiles;
  if (serialFiles != null) {
    newFiles=new File[serialFiles.length + 1];
    for (int i=0; i < serialFiles.length; i++)     newFiles[i]=serialFiles[i];
  }
 else {
    newFiles=new File[1];
  }
  newFiles[newFiles.length - 1]=serialFile;
  serialFiles=newFiles;
}
